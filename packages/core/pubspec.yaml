name: core
description: Shared application logic, data access, and domain models.
version: 0.0.1
resolution: workspace

environment:
  sdk: ^3.12.2

dev_dependencies:
  flutter_lints: ^6.0.0
  test: ^1.25.0
